/* ============================================================
   MODULE 10 — INIT
   First-render calls. MUST be loaded LAST: everything above has to be defined
   before these run (tryRestoreProjectFolder awaits functions from the modules above).
   ============================================================ */

/* ============================================================
   INIT
   ============================================================ */
recomputeAndRender();
tryRestoreProjectFolder();
